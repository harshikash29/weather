name: Test Action

on:
  push:
    branches:
      - main  # Trigger the action on push to the 'main' branch

jobs:
  test:
    runs-on: ubuntu-latest  # The job will run on the latest Ubuntu runner

    steps:
      - name: Checkout code
        uses: actions/checkout@v3  # This step checks out your repository's code

      - name: 22f3002954@ds.study.iitm.ac.in
        run: echo "Hello, world!"  # This step simply prints "Hello, world!" to the log

